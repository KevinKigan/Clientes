import {Region} from './region';

export class Cliente {
    id: number;
    clientName: string;
    lastName: string;
    createAt: string;
    email: string;
    photo: string;
    region: Region;
}
